---
import ProductLayout from '../../../layouts/ProductLayout.astro';
import { products } from '../../../data/products';

const product = products.find((candidate) => candidate.slug === 'ems-c');

if (!product) {
  throw new Error('EMS-C product data was not found.');
}
---

<ProductLayout product={product} allProducts={products} locale="ja" />
