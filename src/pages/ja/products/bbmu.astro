---
import ProductLayout from '../../../layouts/ProductLayout.astro';
import { products } from '../../../data/products';

const product = products.find((candidate) => candidate.slug === 'bbmu');

if (!product) {
  throw new Error('BBMU product data was not found.');
}
---

<ProductLayout product={product} allProducts={products} locale="ja" />
